@namespace fair.ui
@Text<UIcon IconType="IconType.Copy"  @onclick="CopyData"></UIcon>

@code {
    [Parameter]
    public string Text { get; set; }

    async void CopyData()
    {
        await jsRuntime.InvokeVoidAsync("fair.base.CopyText", Text);

    }

}
